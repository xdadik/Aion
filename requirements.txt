# Aion Hand - Core Requirements
# The core framework uses only the Python standard library.
# No external dependencies are required for basic agent functionality.
#
# Optional dependencies (install with: pip install aion-hand[web,messaging,tui,all])
#
# aiohttp>=3.9        # HTTP client/server (web, messaging integrations)
# rich>=13.0          # TUI / pretty output
